# Настройка инфраструктурных параметров. Источник названий и синтаксиса:
# https://docs.spring.io/spring-boot/appendix/application-properties/index.html
spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://186.246.14.209:5432/logdb}
    username: ${SPRING_DATASOURCE_USERNAME:admin}
    password: ${SPRING_DATASOURCE_PASSWORD:admin}
  data:
    mongodb:
      uri: ${SPRING_DATA_MONGODB_URI:mongodb://admin:admin@186.246.14.209:27017/logdb?authSource=admin}
  kafka:
    bootstrap-servers: ${SPRING_KAFKA_BOOTSTRAP_SERVERS:186.246.14.209:9092}

app:
  base-url: ${APP_BASE_URL:http://186.246.14.209:8081}